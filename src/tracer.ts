// import tracer from 'dd-trace';
// import configuration from './modules/config/configuration';

// const config = configuration(); // Call configuration function once

// tracer.init({
//   service: config.ddagent.service,
//   env: config.ddagent.env,
//   version: '1.0.0',
//   logInjection: true,
// });

// export default tracer;
